# i3status configuration file.
# see "man i3status" for documentation.
# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 2
        color_good = "#2AA198"
        color_degraded = "#B58900"
        color_bad = "#DC322F"
}

# Orden optimizado de los módulos
order += "cpu_temperature 0"
order += "cpu_usage"
order += "load"
order += "memory"
order += "disk /"
order += "wireless _first_"
order += "ethernet _first_"
order += "battery all"
order += "volume master"
order += "tztime local"

cpu_temperature 0 {
        format = " %degrees°C"
        path = "/sys/class/thermal/thermal_zone0/temp"
        max_threshold = 80
}

cpu_usage {
        format = " CPU %usage"
}

memory {
        format = " RAM %used / %total"
        threshold_degraded = "1G"
        format_degraded = "MEMORIA < %available"
}

load {
        format = " %1min"
        max_threshold = 5
}

disk "/" {
        format = " %avail"
        prefix_type = "custom"
        low_threshold = 10
        threshold_type = "percentage_avail"
}

wireless _first_ {
        format_up = " %essid (%quality)"
        format_down = " desconectado"
}

ethernet _first_ {
        format_up = " %speed"
        format_down = " desconectado"
}

battery all {
        format = "%status %percentage %remaining"
        format_down = ""
        status_chr = "⚡"
        status_bat = "🔋"
        status_unk = "?"
        status_full = "✓"
        low_threshold = 20
        threshold_type = "percentage"
        integer_battery_capacity = true
        hide_seconds = true
}

tztime local {
        format = " %d/%m/%Y  %H:%M"
}

volume master {
        format = "🔊 %volume"
        format_muted = "🔇 muted"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}
